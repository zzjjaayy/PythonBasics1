# Tuples
# similar to lists, but are immutable
# unlike lists, you can't add, remove, replace the elements.
# you can only get information about the Tuple with methods like count(), index(), etc.

numbersList = [1, 2, 3]
# the above is a list

numbersTuple = (1, 2, 3)
numbersTuple.count(3)
