# comments start with "#"
"""this can be used as a multiline comments, but not recommended as it creates a text constant {which may cause 
errors} """

name = input("What is your name?")
# an input statement whose return value is saved in a variable "name"

print("Hi!" + name)
# this is a string concatenation to add the variable in the string and then print it.

print("Hi!", name)
# a "comma" can also be used to use variables but only outside the inverted commas.
